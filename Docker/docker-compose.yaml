---
version: '3'

services:
  redis-db:
    image: redis:7
    ports:
      - "6379:6379"
    command: --save 1 1
    volumes:
      - ../data:/data
    user: "1000:1000"

  flask-api:
    build:
      context: ../
      dockerfile: ./Docker/Dockerfile
    depends_on:
      - redis-db
    image: kelach/atx_incidents
    ports:
      - "5000:5000"
    command: python3 -u ../src/atx_traffic.py
    volumes:
      - ./config.yaml:/config.yaml
    environment:
      - REDIS_HOSTNAME=redis-db
      
  flask-worker:
    build:
      context: ../
      dockerfile: ./Docker/Dockerfile
    depends_on:
      - redis-db
    image: kelach/atx_incidents
    command: python3 -u ../src/worker.py    
    environment:
      - REDIS_HOSTNAME=redis-db
    restart: unless-stopped


